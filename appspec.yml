version: 0.0
os: linux
files:
  - source: /
    destination: /var/www/api.comiccloud.io
hooks:
  AfterInstall:
    - location: install.sh
permissions:
  - object: /var/www/
    owner: ec2-user
    mode: 775
    type:
        - directory
