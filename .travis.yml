branches:
  only:
    - L5
language: php

php:
  - 5.4
  - 5.5
  - 5.6

sudo: false

before_script:
  - curl -s http://getcomposer.org/installer | php
  - php composer.phar install --test

script: vendor/bin/phpunit